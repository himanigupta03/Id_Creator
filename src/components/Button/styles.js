const { StyleSheet } = require('react-native');

const styles = StyleSheet.create({
    SigninOpacity: {
        width: '90%',
    },

    signindirection: {
        marginTop: 60,
        backgroundColor: 'coral',
        height: 50,
        //borderRadius: 25,
    },

    signindirectionDisable: {
        marginTop: 60,
        backgroundColor: 'gray',
        height: 50,
        //borderRadius: 25,
    },

    title: {
        textAlign: 'center',
        paddingVertical: 12,
        color: 'white',
        fontWeight: 'bold',
        fontSize: 18,
    },
});

export default styles;

